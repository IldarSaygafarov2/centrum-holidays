const accordionBtns = document.querySelectorAll(".accordion");
accordionBtns.forEach((t => {
    if (t.classList.contains('is-open')) {
        // console.log(t.nextElementSibling);
        let e = t.nextElementSibling;
        console.log(t), e.style.maxHeight ? e.style.maxHeight = null : (e.style.maxHeight = e.scrollHeight + "px", console.log(e.style.maxHeight))
    }
    // if (t.classList.contains('is-open')) {
    //     let t = this.nextElementSibling;
    //     console.log(t)
    //     console.log(t), t.style.maxHeight ? t.style.maxHeight = null : (t.style.maxHeight = t.scrollHeight + "px", console.log(t.style.maxHeight))
    // }
    t.onclick = function () {
        this.classList.toggle("is-open");
        let t = this.nextElementSibling;
        console.log(t), t.style.maxHeight ? t.style.maxHeight = null : (t.style.maxHeight = t.scrollHeight + "px", console.log(t.style.maxHeight))
    }
}));